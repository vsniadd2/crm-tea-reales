# Скопируйте в .env и задайте сильные случайные строки (openssl rand -hex 32).
# JWT_SECRET         — подпись access-токена
# JWT_REFRESH_SECRET — подпись refresh-токена
JWT_SECRET=
JWT_REFRESH_SECRET=
